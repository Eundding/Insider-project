spring:
  datasource:
    url: jdbc:mysql://umc-server-db.cngrlpxo9tz5.ap-northeast-2.rds.amazonaws.com/mydb?userSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&autoReconnect=True
    username :
    password :

  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
  mvc:
    path match:
      matching-strategy: ant_path_matcher

server:
  servlet:
    encoding:
      force-response: true


